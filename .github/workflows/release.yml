name: Build & Release

on:
  push:
    tags:
      - 'v*'          # v0.7.0, v1.0.0 등 태그 push 시 릴리즈
  workflow_dispatch:   # 수동 실행도 가능

permissions:
  contents: write      # GitHub Release 생성에 필요

env:
  DOTNET_NOLOGO: true
  DOTNET_CLI_TELEMETRY_OPTOUT: true

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      # diff3.exe 다운로드 (GnuWin32 DiffUtils from SourceForge)
      - name: Download diff3 (GNU DiffUtils)
        shell: pwsh
        run: |
          # GnuWin32 diffutils (stable, well-known source)
          $binUrl = "https://sourceforge.net/projects/gnuwin32/files/diffutils/2.8.7-1/diffutils-2.8.7-1-bin.zip/download"
          $depUrl = "https://sourceforge.net/projects/gnuwin32/files/diffutils/2.8.7-1/diffutils-2.8.7-1-dep.zip/download"
          $extractPath = "$env:TEMP\gnudiff"

          # bin zip 다운로드
          Invoke-WebRequest -Uri $binUrl -OutFile "$env:TEMP\diffutils-bin.zip" -UserAgent "Mozilla/5.0" -MaximumRedirection 10
          Expand-Archive -Path "$env:TEMP\diffutils-bin.zip" -DestinationPath $extractPath -Force

          # dependency zip 다운로드 (libiconv, libintl 등)
          Invoke-WebRequest -Uri $depUrl -OutFile "$env:TEMP\diffutils-dep.zip" -UserAgent "Mozilla/5.0" -MaximumRedirection 10
          Expand-Archive -Path "$env:TEMP\diffutils-dep.zip" -DestinationPath $extractPath -Force

          # diff3.exe 위치 확인
          $diffDir = Join-Path $extractPath "bin"
          Get-ChildItem $diffDir -Filter "*.exe" | ForEach-Object { Write-Host "Found: $($_.Name)" }
          echo "DIFF_TOOLS_DIR=$diffDir" >> $env:GITHUB_ENV

      # Self-contained 빌드 (런타임 포함, 단일 폴더)
      - name: Build (win-x64, self-contained)
        run: |
          dotnet publish src/XlsxMerge/XlsxMerge.csproj `
            -c Release `
            -r win-x64 `
            --self-contained true `
            -p:PublishSingleFile=false `
            -p:IncludeNativeLibrariesForSelfExtract=true `
            -o publish/win-x64

      # diff3.exe, diff.exe를 빌드 출력에 복사
      - name: Bundle diff tools
        shell: pwsh
        run: |
          $diffDir = $env:DIFF_TOOLS_DIR
          if (Test-Path "$diffDir\diff3.exe") {
            Copy-Item "$diffDir\diff3.exe" "publish/win-x64/"
          }
          if (Test-Path "$diffDir\diff.exe") {
            Copy-Item "$diffDir\diff.exe" "publish/win-x64/"
          }
          # 번들 확인
          Get-ChildItem publish/win-x64/*.exe | ForEach-Object { Write-Host $_.Name }

      # ZIP 패키징
      - name: Package release
        shell: pwsh
        run: |
          $version = if ($env:GITHUB_REF_NAME -match '^v') { $env:GITHUB_REF_NAME } else { "dev-$($env:GITHUB_SHA.Substring(0,7))" }
          $zipName = "XlsxMerge-${version}-win-x64.zip"
          Compress-Archive -Path "publish/win-x64/*" -DestinationPath $zipName
          echo "RELEASE_ZIP=$zipName" >> $env:GITHUB_ENV
          echo "RELEASE_VERSION=$version" >> $env:GITHUB_ENV
          Write-Host "Created: $zipName"

      # 빌드 아티팩트 업로드 (모든 트리거에서)
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: XlsxMerge-${{ env.RELEASE_VERSION }}-win-x64
          path: publish/win-x64/
          retention-days: 30

      # GitHub Release 생성 (태그 push 시에만)
      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          name: XlsxMerge ${{ env.RELEASE_VERSION }}
          body: |
            ## XlsxMerge ${{ env.RELEASE_VERSION }}

            ### 변경 사항
            - .NET 9.0 기반
            - ClosedXML 사용 (Excel 설치 불필요)
            - 향상된 diff 성능

            ### 사용법
            1. ZIP 파일을 다운로드하여 압축 해제
            2. `XlsxMerge.exe` 실행
            3. 또는 명령줄: `XlsxMerge.exe -b=base.xlsx -d=mine.xlsx [-s=theirs.xlsx] [-r=result.xlsx]`
          files: ${{ env.RELEASE_ZIP }}
          draft: false
          prerelease: false
